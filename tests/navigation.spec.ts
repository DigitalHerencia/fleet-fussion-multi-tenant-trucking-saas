import { test, expect } from '@playwright/test';

test.describe('Navigation Regression', () => {
  test('Main navigation links are present and route correctly', async ({
    page,
  }) => {
    // TODO: Login as user
    // TODO: Check all main nav links
    // TODO: Click each and expect correct route
  });
});
