import { test, expect } from '@playwright/test';

test.describe('Onboarding Validation', () => {
  test('Shows errors for missing required fields', async ({ page }) => {
    // TODO: Go to onboarding
    // TODO: Submit form with missing fields
    // TODO: Expect validation errors
  });
});
